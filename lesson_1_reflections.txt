How did viewing a diff betwen two versions of a file help you see the bug that was introduced?

To find out whether income changes did it or not.

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?

You can review the history of development and choose version to publish for instance.

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google Docs does?

Git has necessary tools to make version control clearer and easier but it requires specific knowledge to do it.

Why do you think some version control systems, like Git, saving multiple files in one commit, while others, like Google Docs, treat each file separarately?

Because in development process is important to track logical changes of the whole project.

How can you use the commands git log and git diff to view the history of files?

Git log to view history of development process and diff for viewing difference between two commits.

How might using version control make you more confident to make changes that could break something?

You always could checkout previous commit and compare files to see what went wrong.

Now that you have your workspace set up, what do you want to try using Git for?

For working with a project: cloning, commiting, pushing, pulling etc.